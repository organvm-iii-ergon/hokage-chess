# Hokage Chess current-tree audit — 2026-08-31

## Scope

- Repository: `https://github.com/4444J99/hokage-chess`
- Audited branch: `main`
- Audited commit: `6e81fd42e79d94fa3537648aa47fb3e7f0e7c3e4`
- Audited tree: `0c023403873d7f83130be57b5a330a1d287d4cb9`
- Environment: Node.js `v24.19.0`; npm `11.9.0`; Linux
- Audit type: read-only inspection of the cloned upstream tree plus local
  execution of declared package checks; no GitHub mutation

This receipt records the state before the reader-mode documentation changes.

## Tracked implementation inventory

`git ls-files src tests | sort` returned:

```text
src/analytics.ts
src/content-strategy.ts
src/growth.ts
src/index.ts
src/narrative.ts
tests/analytics.test.ts
tests/content-strategy.test.ts
tests/cross-client-bleed-guard.test.ts
tests/growth.test.ts
tests/index.test.ts
tests/narrative.test.ts
```

The source modules export synchronous calculations, classifiers, planners,
interfaces, and string/data-structure builders. The repository does not track
the Python files or package manifest formerly shown in the README.

## Runtime-boundary inspection

The following scans were run against the audited tree:

```bash
rg -n "^(import|const .*require\\()" src
rg -n '"dependencies"|"bin"' package.json
```

Both returned exit code `1` with no matches. The source modules have no imports,
and the package manifest declares neither runtime dependencies nor a command-line
binary. The tracked `src/` tree contains no API client, server, UI, persistence,
authentication, or scheduler module.

`ecosystem.yaml` declares the web-app delivery status as `not_started`.

## Declared-check execution

### Install

`npm ci` failed with `ERESOLVE`: TypeScript `7.0.2` is outside the declared
`@typescript-eslint/eslint-plugin` peer range (`>=4.8.4 <6.1.0`).

`npm ci --legacy-peer-deps` completed as a diagnostic workaround and installed
385 packages. This does not resolve compatibility.

### Tests

`npm test -- --runInBand` discovered six suites but ran zero tests. Every suite
failed during transformation because `ts-jest` reported that TypeScript
`7.0.2` does not expose the JavaScript compiler API it requires.

Observed summary:

```text
Test Suites: 6 failed, 6 total
Tests:       0 total
```

### Build

`npm run build` failed at `src/index.ts:9` with TypeScript error `TS2584`:
`console` is not defined by the configured `ES2022` library set.

### Lint

`npm run lint` failed because ESLint `10.8.1` could not find an
`eslint.config.js`, `eslint.config.mjs`, or `eslint.config.cjs` flat
configuration.

### CI interpretation

`.github/workflows/ci.yml` marks linting and type-checking as
`continue-on-error` and converts test/build command failures into notices with
shell fallbacks. Its success state is therefore not a fail-closed verification
predicate for this checkout.

## Authorship and provenance inspection

Git commit `ed8b20bca0934193c3e39f73712f6a1a05ade0d7` introduced the four
substantive domain modules and their corresponding tests. The commit author is
the repository-owner identity `4444jPPP <etceter4@etceter4.com>` and its message
records `Claude Opus 4.6` as co-author.

`docs/source-materials/PROVENANCE.yaml` records six materials imported from a
pre-existing local Hokage Chess archive, with filenames, hashes, sizes, source
paths, timestamps, and classification confidence.

## Audit conclusion

The audited repository is a prototype TypeScript utility library and
strategy corpus. It contains substantive source and test definitions, but the
current toolchain does not install, test, build, or lint cleanly. It does not
evidence a CLI, dashboard, external platform integration, runnable product
deployment, adoption, revenue, or performance outcomes.
